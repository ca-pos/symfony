{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
   {{ pages_list[pages_list|length-1]}}.html.twig
   <div class="container">
       <h1 class="text-center my-3">Développer une application avec Symfony</h1>
       <h4 class="text-center mb-5">D'après les projets symbnb et blog_cuisine (Symfony version 4.4)</h4>
        <ul class="home">
            {% for i in 1..pages_list|length-1 %}
                <li>
                    <a href="index.php?p={{pages_list[i]}}">
                        {{pages_h1[i]}}
                    </a>
                </li>
            {% endfor %}
        </ul>
   </div>
{% for i in 1..pages_list|length-1 %}
   {{ pages_list[i] }}<br/>
{% endfor %}
{% endblock %}
